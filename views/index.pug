//- extends layout

//- block content
//-   h1= title
//-   p Welcome to #{title} omprakash reddy chamala
//-   button(type="button", class="btn btn-secondary" onclick="double()") I am button 
//-     br
//-   div#double Up the amount
//-   script.
//-     let value = 0;
//-     let power = 1;
//-     function double() {
//-       value += 1;
//-       power *= 2;
//-       document.getElementById('double').innerText = `Two to the power ${value} is ${power}.`;
//-     }
//-       br
//-   a(href='/watercraft') Go to watercraft 
extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  else
    p You are currently logged in as #{user.username}
    a(href="/watercraft") Take me to watercraft
    br
    a(href="/logout") Logout
